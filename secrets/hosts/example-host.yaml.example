# Example host-scoped secret payload for sops-nix.
#
# Copy this file to secrets/hosts/<hostname>.yaml and encrypt it with sops.
wifi:
  ssid: "replace-me"
  password: "replace-me"

services:
  tailscaleAuthKey: "replace-me"
