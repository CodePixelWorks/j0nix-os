# Windows Apps

This directory contains the Windows-app package aggregation used by
`settings.userSettings.<name>.programs.windowsApps.packages`.

There are currently no first-party packages defined here.
