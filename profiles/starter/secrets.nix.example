{
  # Copy this file to secrets.nix and adapt it to the local SOPS setup.
  # The example is never imported automatically.
  defaultSopsFile = ../../secrets/hosts/my-host.yaml;

  age = {
    generateKey = true;
    keyFile = "/var/lib/sops-nix/key.txt";
    sshKeyPaths = [ ];
  };
}
